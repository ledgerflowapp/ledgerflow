# 09 — Implement Contact Merging into Registered Profiles

Category: enhancement
Status: ready-for-agent

## Description
User Story 7 in `spec.md` requires merging duplicate unregistered contacts into newly registered user profiles, transferring or re-linking past transaction history and balances.

## Requirements
- Create `mergeContactToUserProfile` server action in `src/lib/actions/contacts.ts`.
- Wrap the merge operation in a Drizzle ORM database transaction.
- Re-associate `contact_id` transactions and friend ledger splits to the target registered `user_id`.
- Update contact status to merged/linked.

## Acceptance Criteria
- [ ] Server action `mergeContactToUserProfile(contactId, targetUserId)` implemented and protected by session auth.
- [ ] Transaction history correctly re-linked without orphaned records.
- [ ] Unit tests added for contact merge workflow.

## Comments
> *This was generated by AI during triage.*
Triaged as `enhancement` / `ready-for-agent`. Completes User Story 7 from migration spec.
