# 10 — Automated Generation of Scheduled Recurring Transactions

Category: enhancement
Status: ready-for-agent

## Description
User Story 8 in `spec.md` specifies automatic generation of due recurring transactions. Currently `src/lib/actions/recurring.ts` provides manual CRUD actions only.

## Requirements
- Create `processDueRecurringTransactions()` server action / worker function in `src/lib/actions/recurring.ts`.
- Query all active recurring rules where `next_run_date <= NOW()`.
- Generate corresponding transaction entries in a Drizzle transaction and update `next_run_date` according to schedule frequency (`DAILY`, `WEEKLY`, `MONTHLY`, `YEARLY`).
- Add endpoint or route handler to trigger scheduled processing.

## Acceptance Criteria
- [ ] Due recurring transactions correctly create ledger transactions on execution.
- [ ] Schedule dates update atomically.
- [ ] Unit tests added for recurring transaction generator logic.

## Comments
> *This was generated by AI during triage.*
Triaged as `enhancement` / `ready-for-agent`. Completes User Story 8 from migration spec.
