# 08 — Refactor Transaction Drawers to Base UI Data Attributes

Category: bug
Status: ready-for-agent

## Description
`BusinessTransactionDrawer.tsx`, `PersonalTransactionDrawer.tsx`, and `RecurringTransactionDrawer.tsx` contain Radix-style Tailwind class selectors (`data-[state=active]:...` and `data-[state=on]:...`) violating `AGENTS.md` Base UI guidelines.

## Requirements
- Replace `data-[state=active]:...` with `data-active:...` or standard Base UI state variants in `BusinessTransactionDrawer.tsx` and `PersonalTransactionDrawer.tsx`.
- Replace `data-[state=on]:...` with `data-checked:...` or standard state variants in `RecurringTransactionDrawer.tsx`.
- Ensure styling and visual active states match existing design without visual regressions.

## Acceptance Criteria
- [ ] No `data-[state=...]` strings remain in drawer components.
- [ ] Tabs and toggles render correct active/pressed styles.
- [ ] Playwright E2E and Vitest test suites pass cleanly.

## Comments
> *This was generated by AI during triage.*
Triaged as `bug` / `ready-for-agent`. Alignment with repository Base UI standards.
