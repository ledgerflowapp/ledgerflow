# Learner Notes & Preferences

- **Learner Level**: New to testing libraries (Vitest), Better Auth, and server action test patterns.
- **Context**: Working on `ledgerflow` codebase (Next.js 15, Drizzle, Better Auth, Vitest).
- **Goal**: Make an informed design decision for session mocking across test files (`goals.test.ts`, `transactions.test.ts`, `friends.test.ts`, `groups.test.ts`).
